# Optional local development override.
# Place alongside frappe_docker/compose.yaml when testing locally.
version: "3"

services:
  frontend:
    ports:
      - "8080:8080"

  db:
    ports: []  # do not expose MariaDB externally
